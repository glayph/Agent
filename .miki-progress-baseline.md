# Miki progress baseline — 2026-08-19

## Verified
- Local UI at http://127.0.0.1:5173/ loads successfully.
- Single-chat panel is visible; no New Chat/session list controls are present.
- Model selector shows `gemini-2.5-flash`.
- Exact Gemini response test succeeded: `Miki Gemini works`.
- Agent file tool created `/tmp/miki-agent-task.txt`; direct filesystem verification confirmed exact contents `Miki task completed` (19 bytes).
- The agent UI displayed tool calls for `file_write` and `shell_execute`, but its first run stopped with `[Token or context budget exhausted. Stopping.]` before a final report. A follow-up generated a report that incorrectly claimed the read failed, despite direct verification succeeding.

## Backend observations
- `packages/core/src/api/index.ts` contains the single-session ID and FIFO queue patch; live UI shows the single-chat copy.
- `packages/core/src/agent.ts` tracks per-loop budget via `buildAgentTokenBudget()` and stops when `shouldCall` is false.
- Tool results are serialized into a `role: tool` message in `_executeToolInvocation()` and emitted as `tool_result` events.
- Existing `TemporalKnowledgeGraph` already has SQLite entities/events/edges, FTS5, context-window retrieval, dynamic category, access_count, activation, and `recordEntityUsage()`. It needs a clean backend-only NodeGraph API/context-node layer rather than a parallel database.

## Open work
- Find and fix premature/incorrect finalization around token/context budgeting and tool-result reporting.
- Audit remaining backend startup/runtime defects.
- Redesign chat UI as ultra-minimal, Light-only, Anthropic/Codex-style.
- Add a backend-only NodeGraph projection with per-node context and usage-based proximity.
- Add a durable 24/7 worker/recovery design; do not treat the ephemeral sandbox as production hosting.
