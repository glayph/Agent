## packages/config

Status: audited and fixed.

Findings fixed:
- Package tsconfig included Jest test files in library typecheck/build without Jest globals; excluded `src/**/*.test.ts` so the package library emits cleanly.
- Security helper ignored caller-provided environment/options, did not enforce weak-secret rejection, accepted invalid browser origins under wildcard, and failed IPv4-mapped/bracketed IPv6 loopback checks.
- Updated `src/security.ts` to honor canonical/legacy env names and workspace bypass, validate HTTP(S) origins, enforce weak secret values, and normalize loopback addresses.

Validation:
- TypeScript typecheck: passed.
- TypeScript build: passed.
- Jest: 5 suites passed, 28 tests passed.

No Python runtime or dependency was used.

Next package: continue sequentially with the next directory under `packages` only after this package completed.

## packages/cli

Status: audited and fixed.

Findings fixed:
- Node launcher resolved `import.meta.url` as a file path instead of a directory, producing invalid gateway/manifest paths; replaced with `fileURLToPath` + `dirname` and sibling-package fallbacks.
- Windows installer and gateway paths were hard-coded to incorrect repository locations; added actual sibling/repository candidates plus `MIKI_GATEWAY_PATH` override.
- `agent doctor` reported stale paths and advertised unavailable verification commands; it now checks the real CLI manifest, entrypoint, gateway build, Node runtime, and TCP/IP.
- `agent uninstall` previously printed a completion message without deleting anything; it now retains data by default and performs explicit data/log/config deletion only with `--purge`, with truthful status messages.
- Go runtime `Stop()`/TUI Shutdown previously called a detach path that canceled the process context and contradicted its own message; it now terminates the managed process tree through `forceStop`, with a regression test.
- CLI README contained stale @miki/Miki branding and inaccurate package contents/semantics; updated to Agent Miki and current Node.js/Go behavior.

Validation:
- Node syntax check: passed.
- CLI help/version/doctor/install/uninstall/purge and stub gateway start smoke tests: passed.
- Go 1.25 `go test ./...`: passed.
- npm pack dry-run: passed; package includes `agent.js` and 21 package files.

No Python runtime or dependency was used.

Next package: packages/core.

