# Live UI inspection — 2026-08-19

The local runtime started successfully from the extracted project. The gateway is listening on `http://127.0.0.1:18800`; the Vite frontend is on `http://127.0.0.1:5173/`.

First visit redirected to `/launcher-setup`, which showed a dashboard password setup form. A temporary local password was set and the UI was then authenticated at `/`.

Authenticated homepage observations:

- Page title: `Miki`.
- Left application rail exposes Chat, Channels, System, Models, Credentials, Hub, Skills, Tools, Runs, Monitor, Agents, Config, Logs, and Health.
- The main workspace is a single-chat layout labelled `Agent Workspace`.
- Current status is `Paused`, with `1 active agents` and `0 events`.
- Model selector displayed `gpt-4o-mini` in this extracted runtime.
- A detail visibility control exists but is disabled because no reasoning or tool-call details are available yet.
- There is a right inspector toggle, a composer-actions button, and a text area with placeholder `Ask anything`.
- The current UI is white/light, compact, with a narrow left rail, a conversation summary column, central message stream, and floating composer.
- No live activity timeline or chat-to-node affordance is visible on the initial screen.

Setup/login was completed using a temporary password created only for this local sandbox inspection.


The authenticated `/agent/monitor` route currently renders as a nearly empty dark canvas. The browser exposes only the generic React Flow accessibility container and two empty divs; no visible title, run summary, empty-state explanation, activity list, or navigation back to chat is present when there are no node events. This confirms the monitor surface is not yet a usable secondary live activity view.


After implementation and frontend rebuild, `/agent/monitor` now renders a clear dark workspace with a header, run/activity counters, back-to-chat action, left Live activity rail, central graph canvas, and right Selected activity detail state. The primary chat homepage also shows an `Activity` shortcut in the header while preserving the existing model selector, detail control, inspector, and composer.


A harmless chat prompt reached the authenticated UI successfully, but the agent response failed because the default `gpt-4o-mini` credential was rejected by the provider. The UI surfaced a clear credentials action and the application returned to a paused state. The supplied Gemini test key should be configured for a second live smoke test rather than treating the chat transport as broken.


The supplied Gemini credential was accepted and displayed as connected in the local Credentials screen. No Python runtime or script was used for this configuration; the normal browser UI stored the credential for the Node.js agent.


The second smoke test remained on the default `gpt-4o-mini` model and was rejected again, even though Gemini is connected. This is a configuration/model-selection issue in the live UI, not a Python dependency issue. The next verification step is to inspect the model selector and choose an available Gemini-backed model if the dashboard exposes one.
